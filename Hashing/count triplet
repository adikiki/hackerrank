c++

/*

long countTriplets(vector<long> arr, long r) {

 map<int,long>map1,map2;
  long count=0;
 for(int i=0;i<arr.size();i++)
 {
     if(map2.find(arr[i])!=map2.end())
     { 
         count +=map2[arr[i]];
     }

     if(map1.find(arr[i])!=map1.end())
     { 
        map2[arr[i]*r] +=map1[arr[i]];
     }

     map1[arr[i]*r]++;
 }

 return count;
 }
*/
